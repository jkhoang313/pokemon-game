<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @pokemon.name %></title>
  </head>
  <body>
    <h1><%= @pokemon.name.capitalize %></h1>
      <p><strong>Trainer(s):</strong></p>
        <ul>
        <% @pokemon.trainers.each do |trainer| %>
          <li><%= link_to trainer.name.capitalize, trainer_path(trainer)%></li>
        <% end %>
        </ul>

    <!-- <%= form_tag pokemons_path, method: :get do %>
      <%= submit_tag "Back to All Pokemon"%>
    <% end %> -->
    <%= button_to "Back to All Pokemon", pokemons_path, method: :get %>
  </body>
</html>
